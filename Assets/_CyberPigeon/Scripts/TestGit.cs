using UnityEngine;

public class TestGit : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Main branch modif");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
